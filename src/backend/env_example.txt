# QWeather API (used by /api/weather/now and outfit suggestions)
# Copy to backend/.env and fill in real values. Restart the backend after changes.
# Docs: https://dev.qweather.com/

# Use the API Host domain from the QWeather console; do not use https://api.qweather.com
QWEATHER_API_HOST=https://your-api-host.example.com
QWEATHER_KID=your-key-id
QWEATHER_PROJECT_ID=your-project-id
QWEATHER_PRIVATE_KEY_PATH=/absolute/path/to/your-ed25519-private.pem

DASHSCOPE_API_KEY=your-dashscope-api-key
CLASSIFY_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

# Replace {username} and {password} with your Postgres credentials (remove braces)
DATABASE_URL=postgresql://{username}:{password}@localhost:5432/wardrobe_db
SECRET_KEY=your-super-secret-key-change-this-in-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=10080

# Virtual try-on (ComfyUI). Default in code is often http://127.0.0.1:8118
# COMFYUI_SERVER=http://127.0.0.1:8188
